name: Create Jjuza DB Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. v0.1.0)"
        required: true
        type: string

      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean

      os:
        description: "Operating system to build for"
        required: true
        type: choice
        default: all
        options:
          - all
          - linux
          - windows
          - macos

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}

    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            target: linux
            artifact_name: jjuza-db-linux

          - runner: windows-latest
            target: windows
            artifact_name: jjuza-db-windows

          - runner: macos-latest
            target: macos
            artifact_name: jjuza-db-macos

    steps:
      - name: Checkout repository
        if: ${{ inputs.os == 'all' || inputs.os == matrix.target }}
        uses: actions/checkout@v4

      - name: Set up Python
        if: ${{ inputs.os == 'all' || inputs.os == matrix.target }}
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        if: ${{ inputs.os == 'all' || inputs.os == matrix.target }}
        run: |
          python -m pip install --upgrade pip
          pip install .

      - name: Build executable
        if: ${{ inputs.os == 'all' || inputs.os == matrix.target }}
        run: |
          pyinstaller \
            --onefile \
            --name jjuza-db \
            --collect-all pyfiglet \
            --hidden-import=pyodbc \
            jjuza_db/main.py
        shell: bash

      - name: Upload build artifact
        if: ${{ inputs.os == 'all' || inputs.os == matrix.target }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}${{ matrix.os == 'windows-latest' && '.exe' || '' }}

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download Linux executable
        if: ${{ inputs.os == 'all' || inputs.os == 'linux' }}
        uses: actions/download-artifact@v4
        with:
          name: jjuza-db-linux
          path: release

      - name: Download Windows executable
        if: ${{ inputs.os == 'all' || inputs.os == 'windows' }}
        uses: actions/download-artifact@v4
        with:
          name: jjuza-db-windows
          path: release

      - name: Download macOS executable
        if: ${{ inputs.os == 'all' || inputs.os == 'macos' }}
        uses: actions/download-artifact@v4
        with:
          name: jjuza-db-macos
          path: release

      - name: Make Unix executables executable
        run: |
          if [ -f "release/jjuza-db" ]; then
            chmod +x release/jjuza-db
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: Jjuza DB ${{ inputs.version }}
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
          files: |
            release/*